# Requirements for GLPI Ingestion Scripts
# Install with: pip install -r scripts/requirements-glpi.txt

# Required dependencies
beautifulsoup4>=4.12.0
pymysql>=1.1.0
qdrant-client>=1.7.0
sentence-transformers>=2.2.0

# Optional dependencies (recommended)
# These improve functionality but are not required

# ftfy - Fixes text encoding issues (mojibake)
# Without this, basic encoding fixes will be used as fallback
ftfy>=6.0.0

# For better performance
# numpy>=1.24.0  # Usually comes with sentence-transformers
# torch>=2.0.0   # Usually comes with sentence-transformers
